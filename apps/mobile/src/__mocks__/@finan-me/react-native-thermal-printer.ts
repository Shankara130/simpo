/**
 * Mock for @finan-me/react-native-thermal-printer
 * Used in tests to avoid requiring native modules
 */

// Mock the ThermalPrinterModule
export const ThermalPrinterModule = {
  // Add any methods/properties that the module exports
};

// Mock export
export default {};
